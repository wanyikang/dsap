# -*- coding: utf-8 -*-
# x = stack.pop()
# if x is smaller than stack.pop()
#     x = stack.top()
#
# The above method get the big one of the first two elements from top to bottom
# in the stack. So we have 2/3 probability to get the max integer in three. So
# ofcause the big one the first two elements should be assigned to variable x.

