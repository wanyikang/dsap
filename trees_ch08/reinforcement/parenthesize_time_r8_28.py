# -*- coding: utf-8 -*-
# It's just like _height2 method for a general tree. the totle time is amount
# to every node perform the parenthesize code (except for the time running
# children's parenthesize method), and all the other python sentences is
# running in constant time. So the running time is O(n).

