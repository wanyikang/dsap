# -*- coding: utf-8 -*-
# A tree with more than one node, the preorder traveral can not be same order
# with postorder traversal, It's obviously. But preorder traversal may be
# reverse order with postorder traversal. For example, a tree with only two
# nodes.

