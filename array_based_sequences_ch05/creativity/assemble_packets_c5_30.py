# -*- coding: utf-8 -*-
# This is exercise of c5.30
# Use a list to do this in O(n) time.
#
# Initialize a list of length n and all elements is None value, then insert the
# packets into the list by using the packet number as the index. So construct
# the list and iterate the packets will take O(n) time.
