# -*- coding: utf-8 -*-
# First, using a bottom-up construction method to build the maximum-oriented
# heap, that takes O(n) time.
# Second, remove max element k times, that takes k*logn time, so the total time
# is O(n + k*logn).

